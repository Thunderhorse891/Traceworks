{
  "name": "traceworks-netlify",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --test && npm run lint:js",
    "lint:js": "node scripts/check-syntax.mjs",
    "start": "npx netlify-cli dev"
  },
  "dependencies": {
    "nodemailer": "^6.9.15",
    "stripe": "^16.12.0"
  }
}
