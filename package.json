{
  "name": "traceworks",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "check": "node --test && npm run lint:js",
    "lint:js": "node scripts/check-syntax.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "@vercel/kv": "^2.0.0",
    "cheerio": "^1.0.0",
    "nodemailer": "^6.9.15",
    "stripe": "^16.12.0"
  },
  "devDependencies": {
    "vercel": "^37.0.0"
  }
}
